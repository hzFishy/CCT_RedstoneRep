local channelId = 999;
local modemSide = "front" -- Side where the modem is - Option: "bottom", "top", "left", "right", "front", "back"
local redstoneSide = "right" -- Side to send redstone signal - Option: "bottom", "top", "left", "right", "front", "back"


return
{
    channelId = channelId,
    modemSide = modemSide,
    redstoneSide = redstoneSide
}
