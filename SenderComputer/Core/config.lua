local channelId = 999;
local modemSide = "right" -- Side where the modem is -  Options: "bottom", "top", "left", "right", "front", "back"
local redstoneSide = "front" -- Side to read redstone signal - Options: "bottom", "top", "left", "right", "front", "back"


return
{
    channelId = channelId,
    modemSide = modemSide,
    redstoneSide = redstoneSide
}